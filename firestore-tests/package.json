{
  "name": "balance-book-firestore-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Security-rule tests for Balance Book, run against the Firestore emulator.",
  "type": "module",
  "scripts": {
    "test": "node --test rules.test.mjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.0.0"
  }
}